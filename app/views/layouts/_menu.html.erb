<div class="navbar-fixed">
  <ul id="dropdown1" class="dropdown-content">
    <li>
      <%= link_to image_tag('/assets/EN.png', width: 20), {:locale => 'en'} %>
    </li>
    <li>
      <%= link_to image_tag('/assets/FR.png', width: 20), {:locale => 'fr'} %>
    </li>
  </ul>
  <nav>
    <div class="nav-wrapper">
      <%= link_to t('title'), root_path, class: "brand-logo" %>
      <ul class="right hide-on-med-and-down">
        <% if current_user.nil? %>
          <li>
            <%= link_to t('login'), new_user_session_path %>
          </li>
        <% else %>
          <li>
            <%= link_to t('logout'), destroy_user_session_path, :method => :delete %>
          </li>
        <% end %>
        <li>
          <% if current_user.nil? %>
          <% else %>
            <li>
              <%= link_to t('account.compte'), account_conversations_path  %>
            </li>
          <% end %>
        </li>
        <li>
          <a class="dropdown-trigger" href="#!" data-target="dropdown1">
            <%= t('lang') %><i class="material-icons right">arrow_drop_down</i>
          </a>
        </li>
        <li>
          <button class="btn green">
            <%= link_to t('homepage.create_button'), new_article_path %>
          </button>
        </li>
      </ul>
    </div>
  </nav>
</div>

<%- if controller_name != 'visitor' %>
<style>
  nav{
    background: #757575;
  }
</style>
<% end -%>